{
  "name": "discord_instants_player_ui",
  "version": "0.1.0",
  "author": "pinheirolucas",
  "main": "public/electron.js",
  "homepage": "./",
  "build": {
    "publish": {
      "provider": "github"
    }
  },
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.9.7",
    "@material-ui/icons": "^4.9.1",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "axios": "^0.19.2",
    "cross-env": "^10.1.0",
    "electron-is-dev": "^1.1.0",
    "prop-types": "^15.8.1",
    "ramda": "^0.27.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-dropzone": "^10.2.1",
    "react-scripts": "3.4.0",
    "update-electron-app": "^1.5.0",
    "use-persisted-state": "^0.3.0"
  },
  "scripts": {
    "react-start": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "react-build": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "react-test": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts test",
    "react-eject": "react-scripts eject",
    "electron-build": "electron-builder",
    "release": "pnpm react-build && electron-builder --publish=always",
    "build": "pnpm react-build && pnpm electron-build",
    "start": "concurrently \"cross-env BROWSER=none pnpm react-start\" \"wait-on http://localhost:3000 && electron .\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "electron": "^8.1.1",
    "electron-builder": "^22.4.1",
    "wait-on": "^9.1.0"
  }
}
